apiVersion: v1
kind: Service
metadata:
  name: github-insights-pro-service
  namespace: github-insights-pro
spec:
  selector:
    app: github-insights-pro
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP
